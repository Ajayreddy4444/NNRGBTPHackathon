/*const cds = require('@sap/cds');
function Warning(CP,SP){
if(SP<CP)
req.error({'code': 'SP<CP',message:'Selling price cannot be less than cost price'});
}
    

    this.before(['CREATE'], Product, async(req) => {
        price = Warning(req.data.product_cost_price,req.data.product_sell_price);
    });*/
    


